import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    h4: {
      fontSize: '1.8rem', // Adjust as per your need
    },
    h6: {
      fontSize: '1.4rem', // Adjust as per your need
    },
    body2: {
      fontSize: '1.2rem', // Adjust as per your need
    },
  },
});

export default theme;
